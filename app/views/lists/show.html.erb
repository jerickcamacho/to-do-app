<p>
  <strong>Category:</strong>
  <%= @list.id %>
</p>

<p>
  <strong>Description:</strong>
  <%= @list.description %>
</p>

<p>
  <strong>Completed:</strong>
  <%= @list.completed %>
</p>

<%= link_to 'Edit', edit_user_category_list_path(@category, @list) %> |
<%= link_to 'Back', user_category_lists_path(@category) %>
